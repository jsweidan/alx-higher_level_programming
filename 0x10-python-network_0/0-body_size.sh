#!/usr/bin/bash
#a Bash script that takes in a URL, sends a request to that URL, and displays the size of the body of the response
GET request-header-name request-header-value
GET response-header-name response-header-value
POST response-header-name response-header-value
