#!/usr/bin/bash
#a Bash script that takes in a URL, sends a request to that URL, and displays the size of the body of the response
curl -s [GET request-header-name request-header-value]
curl -s [GET response-header-name: response-header-value]
curl -s [POST response-header-name: response-header-value]
